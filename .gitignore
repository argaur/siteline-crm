# Environment — NEVER commit these
.env
bot/.env
landing/dashboard/config.js

# Python
__pycache__/
*.py[cod]
*.pyo
venv/
.venv/

# OS
.DS_Store
Thumbs.db
*.tmp
New Text Document.txt

# Editor
.vscode/
.idea/

# Local agent scratch — must never be published to Pages (landing/ is served wholesale)
.claude/
landing/.claude/

# stacksmith audit output — local tooling artifact, not source
.stacksmith/
