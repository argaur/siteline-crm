// Copy this file to config.js and fill in your values.
// config.js is gitignored — never commit it.

const API_BASE_URL = 'http://localhost:8000';  // Swap to your bot's real API URL in your real config.js (hosting not yet decided — see repo CLAUDE.md)
const BOT_LINK     = 'https://t.me/SitelineCRMbot';
const REFRESH_MS   = 5 * 60 * 1000;             // Auto-refresh every 5 minutes
